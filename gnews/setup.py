from setuptools import setup, find_packages

setup(
    name="GNews",
    version="1.0.0",
    description="A Python package for GNews integration.",
    author="Ihab Rimawi",
    packages=find_packages(),
    install_requires=[
        # Add dependencies here, e.g., 'requests', 'beautifulsoup4>=4.9.0'
    ],
)
